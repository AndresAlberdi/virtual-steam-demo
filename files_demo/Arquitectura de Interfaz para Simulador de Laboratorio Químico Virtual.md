Basándonos en la disposición visual exacta de la mesa de trabajo (Workbench) del simulador que se observa en la captura de pantalla (WhatsApp Image 2026-07-23 at 16.22.05.jpeg) y en el flujo metodológico del proyecto 1, 2, aquí tienes el diseño de arquitectura frontend, la jerarquía de vistas de React y los componentes de interfaz (UI) estructurados con **Tailwind CSS**. \--- \#\#\# 1\. LAYOUT GENERAL DE LA PANTALLA (GRID DE COOPREACIÓN) Para representar de manera exacta la interfaz de la imagen, utilizaremos un grid responsivo de 12 columnas utilizando Tailwind CSS.  \+---------------------------------------------------------------------------------------+ | 1\. HeaderComponent (col-12) | \+--------------------------------------------------+------------------------------------+ | 2\. SidebarMateriales | 3\. CanvasSimulacion | 5\. PanelDetalles (col-3, top) | | (col-3) | (col-6) | | | | \+------------------------------------+ | \+--------------------------+ 6\. InstrumentoAnalisis | | | 4\. PanelControl | (col-3, bottom) | | | (col-6) | | \+-----------------------+--------------------------+------------------------------------+  \--- \#\#\# 2\. JERARQUÍA DE COMPONENTES EN REACT Para tu desarrollo en **Antigravity IDE**, estructuraremos la carpeta del Workbench de la siguiente manera: text src/ ├── context/ │ └── WorkbenchContext.jsx \<-- Manejo de estado global local (React Context) ├── components/ │ └── workbench/ │ ├── HeaderComponent.jsx \<-- Barra de navegación y accesos superiores │ ├── SidebarMateriales.jsx \<-- Barra lateral izquierda de drag-and-drop │ ├── CanvasSimulacion.jsx \<-- Visor central interactivo (WebGL/HTML5 Canvas) │ ├── PanelControl.jsx \<-- Controles deslizantes (Sliders) y botones de acción │ ├── PanelDetalles.jsx \<-- Resumen del experimento y botón de analítica │ └── InstrumentoAnalisis.jsx \<-- El Cromatógrafo interactivo (visor de curvas) └── views/ └── WorkbenchView.jsx \<-- Vista contenedora principal que une todo  \--- \#\#\# 3\. MANEJO DE ESTADO CENTRALIZADO (WorkbenchContext.jsx) Para que los componentes se comuniquen en tiempo real (por ejemplo, que los deslizadores del **PanelControl** alteren la temperatura del display digital en el **CanvasSimulacion** y que el **InstrumentoAnalisis** conozca el estado de la mezcla) 3, 4, definimos el siguiente contexto: javascript import React, { createContext, useContext, useState } from 'react'; const WorkbenchContext \= createContext(); export const WorkbenchProvider \= ({ children }) \=\> { const \[elementosEnMesa, setElementosEnMesa\] \= useState(\[\]); // Vidrio y equipos ensamblados const \[reactivosAñadidos, setReactivosAñadidos\] \= useState(\[\]); // Reactivos vertidos const \[tabActiva, setTabActiva\] \= useState('reactivos'); // 'reactivos' | 'vidrio' | 'equipos' // Parámetros de control const \[temperatura, setTemperatura\] \= useState(20); // de 20°C a 100°C const \[tiempo, setTiempo\] \= useState(0); // en minutos const \[velocidadAgitacion, setVelocidadAgitacion\] \= useState(0); // RPM de 0 a 1000 // Estados físicos de la simulación const \[ejecutando, setEjecutando\] \= useState(false); const \[reaccionCompleta, setReaccionCompleta\] \= useState(false); const \[faseActual, setFaseActive\] \= useState(1); // 1: EPP, 2: Ensamblaje, 3: Parámetros, 4: Reacción, 5: Metacognición return ( {children} ); }; export const useWorkbench \= () \=\> useContext(WorkbenchContext);  \--- \#\#\# 4\. DISEÑO ESPECÍFICO DE COMPONENTES UI (CON TW-CSS) \#\#\#\# A. Vista Contenedora (WorkbenchView.jsx) Organiza el grid de 12 columnas emulando la estética de laboratorio industrial con bordes biselados y tonalidades oscuras de acero inoxidable. jsx import React from 'react'; import { WorkbenchProvider } from '../../context/WorkbenchContext'; import HeaderComponent from './HeaderComponent'; import SidebarMateriales from './SidebarMateriales'; import CanvasSimulacion from './CanvasSimulacion'; import PanelControl from './PanelControl'; import PanelDetalles from './PanelDetalles'; import InstrumentoAnalisis from './InstrumentoAnalisis'; export default function WorkbenchView() { return (  {/\* Barra superior de navegación \*/} {/\* Zona de Trabajo Principal \*/}  {/\* Columna Izquierda: Reactivos y Vidriería \*/}  {/\* Área Central: Visualizador y Sliders \*/}  {/\* Columna Derecha: Reportes e Instrumentación \*/}  ); }  \#\#\#\# B. Barra Lateral Izquierda (SidebarMateriales.jsx) Representa las pestañas azules "Reactivos", "Vidrio" y "Equipos" con su grid correspondiente de íconos interactivos listos para arrastrar (Drag-and-Drop) al Canvas central 3-5. jsx import React from 'react'; import { useWorkbench } from '../../context/WorkbenchContext'; export default function SidebarMateriales() { const { tabActiva, setTabActiva } \= useWorkbench(); const tabs \= \[ { id: 'reactivos', label: 'Reactivos' }, { id: 'vidrio', label: 'Vidrio' }, { id: 'equipos', label: 'Equipos' } \]; const itemsPorTab \= { reactivos: \[ { id: 'hcl', nombre: 'Ácido Clorhídrico', img: '/assets/reactivos/hcl.png' }, { id: 'na2so4', nombre: 'Sulfato de Sodio', img: '/assets/reactivos/na2so4.png' }, { id: 'cuso4', nombre: 'Sulfato de Cobre', img: '/assets/reactivos/cuso4.png' }, { id: 'h2o', nombre: 'Agua Destilada', img: '/assets/reactivos/h2o.png' } \], vidrio: \[ { id: 'vaso', nombre: 'Vaso de Precipitados', img: '/assets/vidrio/vaso.png' }, { id: 'erlenmeyer', nombre: 'Erlenmeyer', img: '/assets/vidrio/erlenmeyer.png' }, { id: 'condensador', nombre: 'Condensador', img: '/assets/vidrio/condensador.png' } \], equipos: \[ { id: 'placa\_calor', nombre: 'Placa Calefactora', img: '/assets/equipos/placa.png' }, { id: 'soporte\_universal', nombre: 'Soporte Universal', img: '/assets/equipos/soporte.png' } \] }; const handleDragStart \= (e, item) \=\> { e.dataTransfer.setData('text/plain', JSON.stringify(item)); }; return (  Reactivos y Materiales  {/\* Botones de Navegación de Pestañas (Tabs) \*/}  {tabs.map((tab) \=\> ( setTabActiva(tab.id)} className={\`py-1.5 text-xs font-bold rounded-t border-t border-x transition-colors duration-150 ${ tabActiva \=== tab.id ? 'bg-gradient-to-b from-sky-600 to-sky-700 text-white border-sky-500' : 'bg-slate-900 text-slate-400 border-slate-800 hover:text-slate-200' }\`} \> {tab.label} ))}  {/\* Grid de Íconos Seleccionables/Arrastrables \*/}  {itemsPorTab\[tabActiva\].map((item) \=\> (  handleDragStart(e, item)} className="flex flex-col items-center justify-center p-2 bg-gradient-to-b from-slate-800 to-slate-850 hover:from-slate-750 border border-slate-700 rounded-md cursor-grab active:cursor-grabbing hover:border-sky-500 transition-all shadow-md group" \>  {item.nombre}  ))}  ); }  \#\#\#\# C. Área de Renderizado Central (CanvasSimulacion.jsx) Muestra el aparato experimental en tiempo real, integrando un display digital retroiluminado sincronizado con la temperatura de la placa calefactora 3, 4, 6\. jsx import React, { useRef, useEffect } from 'react'; import { useWorkbench } from '../../context/WorkbenchContext'; export default function CanvasSimulacion() { const canvasRef \= useRef(null); const { elementosEnMesa, temperatura, ejecutando, reaccionCompleta } \= useWorkbench(); // El Canvas de renderizado maneja la animación WebGL o HTML5 2D de fluidos en tiempo real useEffect(() \=\> { const canvas \= canvasRef.current; const ctx \= canvas.getContext('2d'); let animationFrameId; const render \= () \=\> { ctx.clearRect(0, 0, canvas.width, canvas.height); // Dibujar fondo de laboratorio (imagen difuminada en CSS de fondo) // Lógica de Renderizado del Líquido dentro del Matraz if (ejecutando) { // Simular agitación y burbujas térmicas según la temperatura const nivelBurbujas \= (temperatura \- 20\) / 80; // Intensidad de calor ctx.fillStyle \= \`rgba(0, 112, 255, ${0.4 \+ (temperatura / 200)})\`; // Tono CuSO4 ctx.beginPath(); // Dibujar líquido oscilante ctx.ellipse(250, 260, 45, 15, 0, 0, 2 \* Math.PI); ctx.fill(); // Renderizado de burbujeo cinético for(let i \= 0; i \< nivelBurbujas \* 15; i++) { ctx.fillStyle \= 'rgba(255, 255, 255, 0.6)'; ctx.beginPath(); ctx.arc(220 \+ Math.random() \* 60, 220 \+ Math.random() \* 40, 1 \+ Math.random() \* 3, 0, 2\*Math.PI); ctx.fill(); } } animationFrameId \= window.requestAnimationFrame(render); }; render(); return () \=\> window.cancelAnimationFrame(animationFrameId); }, \[ejecutando, temperatura\]); return (  {/\* Display LED digital de la Placa Calefactora (Sincronizado al estado) \*/}  {temperatura.toFixed(0)}°C  ); }  \#\#\#\# D. Panel de Control de Parámetros (PanelControl.jsx) Agrupa los deslizadores analógicos de control físico y los dos grandes botones de acción "Iniciar Experimento" (verde) y "Detener" (rojo) ubicados debajo de la zona de simulación. jsx import React from 'react'; import { useWorkbench } from '../../context/WorkbenchContext'; export default function PanelControl() { const { temperatura, setTemperatura, tiempo, setTiempo, velocidadAgitacion, setVelocidadAgitacion, ejecutando, setEjecutando } \= useWorkbench(); return (  {/\* Controles deslizantes \*/}  {/\* Deslizador de Temperatura \*/} Temperatura:{temperatura}°C setTemperatura(Number(e.target.value))} className="col-span-7 h-1.5 bg-slate-900 rounded-lg appearance-none cursor-pointer accent-sky-500" /\>  {/\* Deslizador de Tiempo \*/} Tiempo: {Math.floor(tiempo / 60)}:{(tiempo % 60).toString().padStart(2, '0')} min  setTiempo(Number(e.target.value))} className="col-span-7 h-1.5 bg-slate-900 rounded-lg appearance-none cursor-pointer accent-sky-500" /\>  {/\* Botones de acción del sistema \*/}  setEjecutando(true)} disabled={ejecutando} className="w-full py-2 bg-gradient-to-b from-emerald-600 to-emerald-700 hover:from-emerald-500 active:to-emerald-800 disabled:opacity-45 text-white font-bold text-sm rounded shadow border border-emerald-500/30 transition-all" \> Iniciar Experimento setEjecutando(false)} disabled={\!ejecutando} className="w-full py-2 bg-gradient-to-b from-rose-600 to-rose-700 hover:from-rose-500 active:to-rose-800 disabled:opacity-45 text-white font-bold text-sm rounded shadow border border-rose-500/30 transition-all" \> Detener  ); }  \#\#\#\# E. El Cromatógrafo Analítico (InstrumentoAnalisis.jsx) Representa el panel inferior derecho de análisis espectrométrico. Muestra una gráfica con un pico estequiométrico correspondiente a la pureza obtenida y el teclado numérico de control 6\. jsx import React from 'react'; import { useWorkbench } from '../../context/WorkbenchContext'; export default function InstrumentoAnalisis() { const { reaccionCompleta, ejecutando } \= useWorkbench(); return (  Cromatógrafo Analítico  {/\* Pantalla del Cromatógrafo (Graficador de Picos) \*/}  {ejecutando ? (  {/\* Animación del gráfico del pico analítico en tiempo real \*/}  {/\* Pico del CuSO4 \*/}  Analizando Muestra...  ) : (  Instrumento Offline  )}  {/\* Teclado Físico Digital de Control \*/}  {\['7', '8', '9', 'CLR', '4', '5', '6', 'ENT', '1', '2', '3', 'ESC', '0', '.', '+', '-'\].map((key) \=\> ( {key} ))}  ); }  \--- \#\#\# Explicación general del código y siguientes pasos Este mapeo de vistas de React está diseñado con **Tailwind CSS** para estructurar fielmente la mesa de trabajo (Workbench) del prototipo virtual de química, basándose rigurosamente en la imagen cargada de la pantalla. Los componentes utilizan un estado reactivo local administrado por un contexto (WorkbenchContext.jsx) de costo cero, perfectamente apto para operar dentro de las limitaciones gratuitas de Firestore y Firebase Hosting (plan Spark) en **Antigravity IDE** 7\.   
